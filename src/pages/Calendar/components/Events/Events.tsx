import React from "react";
import { EventProps } from "../Event/Event";

export type EventsProps = {
  children?: React.ReactElement<EventProps>[];
};

export const Events = (props: EventsProps) => {};
